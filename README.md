# React-Crud-Refresher

## Requirements
- Need a quick refresher course for react, 
- react client, 
- nodejs / express backend 
- in memory data (no database), 
- need to implement a basic crud action: list data in table, edit, delete, 
- create - basic css is fine, no tailwind or bootstrap, 
- usestate and useeffect, with explanation 
- Please create step by step instructions with code samples, so I can create this refresher

## Project structure

- react-crud-refresher
    - server
        - index.ts
    - client
        - (Vite React app)


## Tasks
- [x] server
  - npm init -y
  - npm i express cors
  - npm i -D typescript ts-node @types/express @types/cors nodemon
  - npx tsc --init
  - 
- [x] client
  - npm init -y
  - npm i react react-dom vite @vitejs/plugin-react
  - npm i -D typescript ts-node @types/react @types/react-dom @types/node @vitejs/plugin-react 
  - npx tsc --init
- [x] run and verify
  - cd server && npm i && npm run dev
  - cd client && npm i && npm run dev
  - open http://localhost:5173
    
